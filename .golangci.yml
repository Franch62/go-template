run:
  timeout: 5m
  tests: true
  skip-dirs:
    - "test"
  skip-files:
    - "_test.go"

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - revive
    - gofmt
    - goimports

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0 
